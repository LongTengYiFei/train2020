To write a program, you need to traverse the specified directory. Requirements:
1. Display directory item name and status information
2. Nested traversal of subdirectories

your command:./test dir_name
